let isRunning = false;
let hierarchy = [];

// Create Game Object, with name, position, rotation, size, isEnabled, components

function AddObjectToHierarchy(object, position, rotation, scale){
    return object // Return the object reference inside the hierarchy
}

function RemoveObjectFromHierarchy(id){} // Free memory please

function StartGame(){}

function StopGame(){}


